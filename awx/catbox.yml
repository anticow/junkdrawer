---
- name: Run container
  hosts: catbox
  gather_facts: no
  tasks:
    - name: restart container
      docker_compose:
        project_src: /opt/compose
        files:
        - compose.yml
        state: restarted